#include <bits/stdc++.h>
#include <iostream>
#include <cmath>
using namespace std;

float f(x)
{
    return pow(x, 3) - pow(x, 2) + x - 1;
}

int main()
{
    float a,b,c,z;
    cout << "Введите a,b и c" << endl;
    cin >> a >> b >> c;
    2 = f(2*a) + f(b+c);
    cout << "2 = " << 2;
    return 0;
}